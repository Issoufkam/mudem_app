import 'package:flutter/material.dart';

class ContributionList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Ajoutez ici l'interface de la liste des cotisations

    return Container(
      // Le contenu de votre liste des cotisations serait ici
    );
  }
}
